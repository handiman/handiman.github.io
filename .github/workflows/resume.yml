name: Update Gist
on: 
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - run: |
          ./update-gist.sh ${{ secrets.AUTH_TOKEN }}$AUTH_TOKEN 3881a8e890dd1032cce3f5e655859717
